<UserControl x:Class="OpenFortiVPN.GUI.Controls.StatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:OpenFortiVPN.GUI.Converters"
             x:Name="Root">

    <UserControl.Resources>
        <converters:ConnectionStateToBrushConverter x:Key="StateToBrush" />
        <converters:ConnectionStateToIconConverter x:Key="StateToIcon" />

        <!-- Pulsing animation for connecting states -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="pulseCircle"
                             Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.3"
                             Duration="0:0:1"
                             AutoReverse="True" />
        </Storyboard>
    </UserControl.Resources>

    <Grid Width="48" Height="48">
        <!-- Outer glow for connected state -->
        <Ellipse x:Name="glowCircle"
                 Width="48" Height="48"
                 Opacity="0.15"
                 Fill="{Binding State, ElementName=Root,
                        Converter={StaticResource StateToBrush}}" />

        <!-- Main indicator circle -->
        <Ellipse x:Name="pulseCircle"
                 Width="32" Height="32"
                 Fill="{Binding State, ElementName=Root,
                        Converter={StaticResource StateToBrush}}" />

        <!-- Icon glyph -->
        <TextBlock Text="{Binding State, ElementName=Root,
                          Converter={StaticResource StateToIcon}}"
                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                   FontSize="14"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
    </Grid>
</UserControl>
