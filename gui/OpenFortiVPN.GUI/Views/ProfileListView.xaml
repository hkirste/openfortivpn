<UserControl x:Class="OpenFortiVPN.GUI.Views.ProfileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:OpenFortiVPN.GUI.ViewModels"
             xmlns:converters="clr-namespace:OpenFortiVPN.GUI.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ProfileListViewModel}"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,28">
        <StackPanel MaxWidth="700">

            <!-- Header -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="VPN Profiles"
                           Style="{StaticResource TitleLargeStyle}" />

                <Button Grid.Column="1"
                        Content="Import"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding ImportProfileCommand}"
                        Margin="0,0,8,0" />

                <Button Grid.Column="2"
                        Content="New Profile"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CreateNewCommand}" />
            </Grid>

            <!-- Empty State -->
            <Border Visibility="{Binding HasProfiles, Converter={StaticResource BoolToVis},
                                         ConverterParameter=Invert}"
                    Style="{StaticResource CardStyle}"
                    Padding="40">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="&#xE774;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="48"
                               Foreground="{StaticResource TextTertiaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16" />
                    <TextBlock Text="No VPN profiles yet"
                               Style="{StaticResource SubtitleStyle}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8" />
                    <TextBlock Text="Create a new profile or import an existing openfortivpn configuration file."
                               Style="{StaticResource BodyStyle}"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               MaxWidth="400" />
                </StackPanel>
            </Border>

            <!-- Profile Cards -->
            <ItemsControl ItemsSource="{Binding Profiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}"
                                Margin="0,0,0,8"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Profile Info -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}"
                                               Style="{StaticResource SubtitleStyle}"
                                               Margin="0,0,0,4" />
                                    <TextBlock Style="{StaticResource CaptionStyle}">
                                        <Run Text="{Binding GatewayHost, Mode=OneWay}" />
                                        <Run Text=":" />
                                        <Run Text="{Binding GatewayPort, Mode=OneWay}" />
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource CaptionStyle}"
                                               Margin="0,2,0,0">
                                        <Run Text="User:" />
                                        <Run Text="{Binding Username, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Content="Edit"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding DataContext.EditProfileCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,4,0"
                                            MinWidth="60" Padding="12,8" />

                                    <Button Content="Export"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding DataContext.ExportProfileCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,4,0"
                                            MinWidth="60" Padding="12,8" />

                                    <Button Content="Copy"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding DataContext.DuplicateProfileCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,4,0"
                                            MinWidth="60" Padding="12,8" />

                                    <Button Content="Delete"
                                            Style="{StaticResource DangerButtonStyle}"
                                            Command="{Binding DataContext.DeleteProfileCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            MinWidth="60" Padding="12,8" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
